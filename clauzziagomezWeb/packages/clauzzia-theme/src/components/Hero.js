import React from 'react'
import {connect, styled} from 'frontity'

const Hero = ({state})=>{
    
    return(
        <>
            <h1>Estamos en hero</h1>
        </>
    )
}
 export default connect(Hero)

 const containerHero = styled.div`
    
 `